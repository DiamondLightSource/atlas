import { Container } from "@mui/material";

const Workflows = () => {
  return (
    <Container
      maxWidth={false}
      sx={{ minHeight: "100vh", mt: 3, mb: 3 }}
    ></Container>
  );
};

export default Workflows;
